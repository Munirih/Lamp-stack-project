# ============================================================
# .env.example
# ============================================================
# Copy this file to .env and fill in your real values.
# NEVER commit your .env file to GitHub — it is in .gitignore.
# In production (EC2), these are set as real environment variables
# or injected by the CI/CD pipeline from GitHub Secrets.
# ============================================================

# -- Database (MySQL on Private Subnet EC2) --
DB_HOST=10.0.2.xx          # Private IP of your DB server (from AWS console)
DB_PORT=3306               # Default MySQL port
DB_NAME=lampdb             # The database name you created
DB_USER=lampuser           # MySQL user (NOT root)
DB_PASSWORD=yourpassword   # Strong password — store this in GitHub Secrets

# -- Flask --
FLASK_ENV=production       # Never use 'development' on a real server
SECRET_KEY=changeme        # Used for session security — make it long and random
